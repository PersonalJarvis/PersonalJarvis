"""Copilot features reached by explicit command: Material (order -> deck) and
Workflow (observe PC work -> automation candidates). Teacher mode lives in
``jarvis.teacher``. The brain manager only routes a matched command here.
"""
