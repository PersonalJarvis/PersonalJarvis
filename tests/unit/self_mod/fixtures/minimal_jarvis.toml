# Personal Jarvis — Test-Fixture für Phase-7.2 Atomic-Writer-Tests.
# Dieser Kommentar muss alle 100 Mutationen überleben (Plan-AC §7.2).

[profile]
name = "test-runner"
language = "de"  # de | en | auto

[tts]
# Default-Provider für Voice-Tests
provider = "gemini-flash-tts"
voice_de = "Charon"
voice_en = "Charon"
speed = 1.0
language_code = "de-DE"

[stt]
provider = "faster-whisper"

[brain]
primary = "claude-api"

[ui]
tray_enabled = true

# Trailing-Kommentar — auch dieser muss erhalten bleiben.
